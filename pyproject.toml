[tool.poetry]
name = "pokemon-ebay-tracker"
package-mode = false

[tool.poetry.dependencies]
python = ">=3.10"
beautifulsoup4 = "*"
requests = "*"
django = "*"
django-mathfilters = "*"
pytz = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
